# A 股技能库整改计划任务清单

> 生成时间：2026-09-04 ｜ 范围：`a-stock-*` 各技能 ｜ 主线：**短线为主、基本面仅作小参考、风险放大** + 各模型补强自学习闭环。

---

## 一、总原则

1. **短线为主**：所有模型的标签、预测窗口、命中口径，统一以 T+1~T+3（最多 T+5）短线收益为准，不追求中长期。
2. **基本面小参考**：基本面只作过滤/降权，不参与主打分；**风险要放大**——财务/立案/退市风险/一字跌停直接一票否决或强制降级。
3. **情绪前置**：市场情绪（封板率、连板高度、炸板率）是短线第一闸门，情绪退潮时所有模型整体降级。
4. **自学习闭环**：每个模型遵循「**产出 → 验证 → 反推参数 → 回写 → 更准**」的通用架构（复刻龙虎榜已有的 walk-forward 模式）。

---

## 二、整改任务清单

### P0 — 首板炸板洗盘（board-washout）【最薄弱，收益最大】✅ 已完成

现状：纯六因子打分、权重拍脑袋、零验证闭环。

- [x] **T1.1** 新增 `scripts/verify.py`：读取 `screen_{T}.json` 候选，结合历史 K 线回填 T+1/T+2/T+3 实际涨幅、涨停、最高涨幅，累计写入 `verify_history.csv`。
- [x] **T1.3** 新增 `scripts/evolve.py`：用累计样本反推六因子权重，回写 `params_best.json`。
- [x] **T1.4** `screen_washout.py` 读取 `params_best.json` 覆盖硬编码权重（缺省回落内置权重）。
- [ ] **T1.2** 新增「首板炸板洗盘-验证复盘」报告生成（命名 `首板炸板洗盘-验证复盘-{date}.md`）——待补独立复盘报告（当前 verify.py 仅控制台汇总 + CSV）。

### P0 — 一阳指（a-stock-yiyangzhi）【有回放、缺闭环】✅ 已完成

现状：`optimize.py` 逐日回放但只观察、不自动回写；无 `verify` 闭环。

- [x] **T2.1** 新增 `verify` 子命令：回填已出信号标的的真实 T+1~T+5 命中，累计 `verify_history.csv`。
- [x] **T2.2** 扩展 `optimize.py`：全参数搜索，按命中率择优**自动回写** `params_best.json`。
- [x] **T2.3** `scan.py`/`judge.py` 经 `_common._apply_params()` 读取 `params_best.json` 覆盖默认常量（缺省回落）。
- [x] **T2.4** 预测窗口 T+5 → 改 T+1/T+3/T+5 三档命中率，更贴合短线。

### P1 — 行情复盘轮动（a-stock-operator-v2）【新增：次日轮动主线推荐 + 强化自学习】✅ 已完成

现状：`gen_rotation_v2.py` 有四象限 + 多因子趋势榜，但 `WEIGHTS_T` 硬编码、无次日验证、无自学习。

- [x] **T4.1** 新增 `scripts/recommend_rotation.py`：消费 `rotation_{T}.json` + `boards_15d/fund_15d/zt_15d`，融合「位置、资金、强度、情绪、热度」多因子打分，输出**次日轮动主线题材/板块推荐**（主线/接力/潜伏三层 + 推荐理由 + 风险提示）。
- [x] **T4.2（强化自学习）** 新增 `scripts/rotation_verify.py`：回填次日真实涨幅，累计 `rotation_history.csv`。
- [x] **T4.3（强化自学习）** 新增 `scripts/rotation_evolve.py`：用累计样本（含情绪状态特征）反推各因子权重，回写 `rotation_params.json`。
- [x] **T4.4** `recommend_rotation.py` 优先读 `rotation_params.json`，缺省用内置权重，形成「推荐→验证→反推→更准」闭环。
- [x] **T4.5** 情绪温度计权重校准：新增 `scripts/emotion_calibrate.py`，用已累积 emotion_*.json + 次日 market 数据计算「当日温度/各因子 与 次日涨停晋级率」的 Spearman 相关，输出 `情绪温度计权重校准-{date}.md` 观测报告。**轻量定位**：情绪温度计是日内状态描述而非次日预测模型，样本<10 日不给出调参建议（避免过拟合），正式校准建议 ≥30 日后再人工微调 `TEMPERATURE_W`。

### P1 — 龙虎榜（a-stock-lhb-rec）【已有闭环，做精细化】✅ 已完成（模型择优 + 暴雷过滤）

现状：已具备时间外 AUC 门控 / walk-forward / 防泄漏 / 时间衰减。

- [x] **T3.3** 模型择优融合：`train.py` 在同一时间外测试集上比较 LR vs GBDT，GBDT 明显更优且样本≥300 时自动采用 GBDT 作生产模型（否则回落 LR），`model_kind` 记入 meta/history。（原代码已有 GB 作「对照」，本次升级为「择优生产」）
- [x] **T3.4（补充）** 暴雷一票否决：`recommend.py` 对 ST/退市/立案/违规/预亏等风险股强制剔除（`_risk` 标记）。
- [~] **T3.1 标签多分类**：**暂不做（负优化）**。龙虎榜样本稀疏（每日上榜几十只），单一 `any_zt` 已是 T+1/T+2/T+3 任一涨停；拆成 T+1/T+2/T+3 分档会进一步稀释正例，多分类模型反而更易过拟合。保留现有 `D1/D2/D3` 字段（已含分档信息）供后续观察，不在生产模型上强制多分类。
- [~] **T3.2 情绪分桶**：**暂不做（负优化）**。已有 `MARKET_ZT`（市场涨停率）情绪特征进入模型，再按封板率/连板高度分桶训练会稀释训练集、破坏时间外验证的连续性。情绪已作为连续特征承载，无需分桶。

### P2 — 三模型统一情绪闸门 + 暴雷过滤器【横向共通】✅ 已完成（暴雷过滤部分）

- [x] **T5.1** 新增可复用模块 `_risk_gate.py`（技能库根目录，纯函数零依赖）：`emotion_gate()` 情绪闸门（封板率/炸板率/连板高度判定冰点/分歧/梯队弱并提示降级）+ `risk_level()`/`apply_gate()` 暴雷过滤（ST/退市/立案/处罚/预亏/一字跌停等）。
- [x] **T5.2** 暴雷过滤器接入三处：
  - board-washout `collect_stocks.py`：候选过滤加 `is_risk_reason`（立案/退市风险等，与 `is_st` 并列）。
  - yiyangzhi `_common.py`：`fetch_universe` 剔除 `is_risk_name`（ST/*ST/退），比原仅 ST 更全。
  - lhb-rec `recommend.py`：`_common.py` 加 `risk_level`，推荐输出对暴雷股一票否决（`_risk` 标记，纳入观察候选强制排除）。
- [ ] **T5.3** 情绪闸门 `emotion_gate()` 尚未接入各模型主流程（需各模型把封板率/炸板率/连板高度传入调用）——作为后续增强，当前先提供可复用模块 + 暴雷过滤落地。

---

## 三、实施优先级

| 顺序 | 任务 | 价值 | 状态 |
|:---:|:---|:---|:---:|
| ① | P0 首板洗盘次日验证（T1） | 最薄弱，收益最大 | ✅ 已完成 |
| ② | P0 一阳指全参数自动回写（T2） | 有回放基础，改动小 | ✅ 已完成 |
| ③ | P1 轮动推荐 + 强化自学习（T4） | 用户点名新增 | ✅ 已完成 |
| ④ | P2 统一情绪闸门 + 暴雷过滤（T5） | 风险放大，横向兜底 | ✅ 已完成（暴雷部分） |
| ⑤ | P1 龙虎榜标签精细化（T3） | 锦上添花 | ✅ 已完成（模型择优+暴雷；多分类/分桶判为负优化不做） |
| ⑥ | P1 情绪温度计校准（T4.5） | 低优先 | ✅ 已完成（轻量观测工具） |

---

## 四、风险与约束

- **样本稀疏过拟合**：自学习须设最小样本门槛（如 n≥30），不足维持默认参数，不强行调参。
- **情绪周期漂移**：参数/权重须做时间衰减或滑动窗口，避免旧周期规律误导新周期。
- **标签「可买入性」陷阱**：命中口径用「涨停/最高涨幅」而非「收盘涨幅」，避免次日一字/高开不可买入的假命中。
- **防泄漏**：训练样本只用 T 日及之前可得信息，杜绝未来函数。

---

## 五、待确认

请确认从哪一项开始实施（推荐按 ①→⑥ 顺序，或指定某一项）。
